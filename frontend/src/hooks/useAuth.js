/**
 * Re-export useAuth hook from AuthContext for convenience
 * This allows importing from hooks directory: import { useAuth } from '../hooks/useAuth'
 */
export { useAuth } from '../contexts/AuthContext';
